/target
Cargo.lock
*.swp
.DS_Store

# Parity harness: upstream source + build artifacts + ephemeral runs.
# The scripts rebuild these on demand — see parity/scripts/build-upstream.sh.
/parity/upstream/lastz-*/
/parity/upstream/bin/
/parity/out/
# Real-world fixtures downloaded by parity/scripts/fetch-genomes.sh.
# Too large (~50 MB each for chr21, ~16 KB for chrM) to commit; the
# script is the source of truth.
/parity/corpus/real/
